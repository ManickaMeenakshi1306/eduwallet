/* STUNNING UI COMPONENTS - REFINED */

// 1. Init everything
document.addEventListener('DOMContentLoaded', () => {
    // Standard initialization for all pages
    if (window.lucide) lucide.createIcons();
});
